const {useEffect,useMemo,useRef,useState}=React;
const CFG=window.BANDCENTER_CONFIG||{};
const ENSEMBLES=["Elementary Ensemble","JV Band","Varsity Band"];
const INSTRUMENTS=["Flute","Clarinet","Saxophone","Trumpet","Low Brass","Percussion"];
const SIZES=[8,16,32,64];
const SOUND={start:"the-espn-sound.mp3",nba:"nba_on_espn.mp3",espn:"espntheme.mp3",deep:"he-could-go-all-the.mp3",victory:"nfl-draft-chime.mp3",applause:"applause.mp3"};
const ADJ=["Blazing","Golden","Electric","Fearless","Turbo","Mighty","Ice","Wild","Rocket","Prime","Neon","Storm","Super","Sonic","Iron","Sky","Royal","Crimson","Phantom","Power","Major","Rising","Rapid","Cosmic","Thunder"];
const NOUN=["Ace","Wolf","Comet","Titan","Bolt","Hawk","Viper","Maestro","Rocket","Rhino","Falcon","Dragon","Chief","Panther","Legend","Ranger","Cyclone","Captain","Hammer","Knight","Phantom","Star","Cobra","Firebird","Whippet"];
const uid=()=>Math.random().toString(36).slice(2)+Date.now().toString(36);
const today=()=>new Date().toISOString().slice(0,10);
const clamp=(n,a,b)=>Math.max(a,Math.min(b,Number(n)||0));
const hash=s=>[...String(s)].reduce((a,c)=>((a<<5)-a+c.charCodeAt(0))|0,0);
function generatedMoniker(s,salt=0){const h=Math.abs(hash(`${s.id||s.name}|${s.instrument}|${salt}`));return `${ADJ[h%ADJ.length]} ${NOUN[Math.floor(h/ADJ.length)%NOUN.length]}`;}
const defaultRules={attempt:10,s7:25,s8:40,s9:60,s95:80,s10:100,personalBest:20,improveOne:15,tournamentEntry:20,roundWin:25,eliteEight:50,finalFour:75,runnerUp:125,champion:200};
function emptyState(){return{version:2,meta:{seasonName:CFG.seasonName||"Beginning Band Championship Series",schoolName:CFG.schoolName||"Okeene Whippet Barking Battalion",updatedAt:Date.now()},students:[],challenges:[],tournaments:[],rules:{...defaultRules},settings:{sound:true,volume:.7,publicNameStyle:"moniker",activeEnsemble:"Elementary Ensemble"},legacy:{}}}
function migrate(raw){if(!raw||typeof raw!=="object")return emptyState();if(raw.version===2){const s={...emptyState(),...raw,meta:{...emptyState().meta,...raw.meta},rules:{...defaultRules,...raw.rules},settings:{...emptyState().settings,...raw.settings}};s.students=(s.students||[]).map(x=>({...x,moniker:x.moniker||generatedMoniker(x),ensemble:x.ensemble||"Elementary Ensemble",active:x.active!==false,bandBucks:Number(x.bandBucks||0)}));return s;}
 const s=emptyState();s.students=(raw.students||[]).map(x=>({...x,ensemble:x.ensemble||"Elementary Ensemble",moniker:x.moniker||generatedMoniker(x),active:true,bandBucks:Number(x.bandBucks||0)}));
 s.challenges=(raw.chairTests||[]).map(t=>({id:t.id||uid(),name:t.name||"Legacy Playing Challenge",date:t.date||today(),ensemble:t.ensemble||"Elementary Ensemble",scores:t.scores||{},archived:!!t.isArchived,legacy:true}));
 s.tournaments=(raw.tournaments||[]).map(t=>({...t,ensemble:t.ensemble||"Elementary Ensemble",archived:!!t.isArchived,legacy:true}));s.legacy={sectionPoints:raw.sectionPoints||{},chairOrders:raw.chairOrders||{}};return s;}
function scoreBase(score,r){const x=Number(score);if(!Number.isFinite(x)||x<=0)return 0;let p=r.attempt;if(x>=10)p+=r.s10;else if(x>=9.5)p+=r.s95;else if(x>=9)p+=r.s9;else if(x>=8)p+=r.s8;else if(x>=7)p+=r.s7;return p;}
function challengeHistory(state,studentId){return(state.challenges||[]).filter(c=>!c.archived&&c.scores&&c.scores[studentId]!==""&&c.scores[studentId]!=null).map(c=>({id:c.id,date:c.date||"",score:Number(c.scores[studentId]),name:c.name})).filter(x=>Number.isFinite(x.score)&&x.score>0).sort((a,b)=>a.date.localeCompare(b.date));}
function challengePoints(state,studentId){const h=challengeHistory(state,studentId),r=state.rules;let total=0,best=-Infinity,streak=0,maxStreak=0,last=null,pb=0;h.forEach(a=>{let p=scoreBase(a.score,r);if(a.score>best){if(best>-Infinity){p+=r.personalBest;pb++;}best=a.score;}if(last!=null&&a.score-last>=1)p+=r.improveOne;if(last!=null&&a.score>last){streak++;maxStreak=Math.max(maxStreak,streak)}else streak=0;total+=p;last=a.score;});return{total,best:best===-Infinity?0:best,attempts:h.length,pb,maxStreak,last:last||0,history:h};}
function tournamentParticipants(t){const set=new Set();(t.rounds||[]).forEach(r=>(r.matches||[]).forEach(m=>{if(m.p1)set.add(m.p1);if(m.p2)set.add(m.p2)}));return set;}
function tournamentStats(state,studentId){const r=state.rules;let total=0,wins=0,titles=0,runnerUps=0,finalFours=0,eliteEights=0,entries=0;for(const t of state.tournaments||[]){if(t.archived)continue;const parts=tournamentParticipants(t);if(!parts.has(studentId))continue;entries++;total+=r.tournamentEntry;(t.rounds||[]).forEach((round,ri)=>{(round.matches||[]).forEach(m=>{if(m.winner===studentId){wins++;total+=r.roundWin;}})});const rounds=t.rounds||[];const champ=t.championId||((rounds.at(-1)?.matches||[])[0]?.winner);if(champ===studentId){titles++;total+=r.champion;}const final=(rounds.at(-1)?.matches||[])[0];if(final&&(final.p1===studentId||final.p2===studentId)&&champ!==studentId){runnerUps++;total+=r.runnerUp;}const sf=rounds.at(-2);if(sf&&sf.matches.some(m=>m.p1===studentId||m.p2===studentId)){finalFours++;total+=r.finalFour;}const qf=rounds.at(-3);if(qf&&qf.matches.some(m=>m.p1===studentId||m.p2===studentId)){eliteEights++;total+=r.eliteEight;}}
 return{total,wins,titles,runnerUps,finalFours,eliteEights,entries};}
function statsFor(state,student){const c=challengePoints(state,student.id),t=tournamentStats(state,student.id);return{...c,...t,points:c.total+t.total};}
function allStats(state,ensemble){return state.students.filter(s=>s.active!==false&&s.ensemble===ensemble).map(s=>({student:s,...statsFor(state,s)})).sort((a,b)=>b.points-a.points||b.best-a.best||a.student.moniker.localeCompare(b.student.moniker));}
function useAudio(state){const ref=useRef({});useEffect(()=>{Object.entries(SOUND).forEach(([k,v])=>ref.current[k]=new Audio(v));},[]);return{play:k=>{if(!state.settings.sound)return;const a=ref.current[k];if(!a)return;Object.values(ref.current).forEach(x=>{try{x.pause();x.currentTime=0}catch{}});a.volume=clamp(state.settings.volume,0,1);a.play().catch(()=>{});},stop:()=>Object.values(ref.current).forEach(a=>{try{a.pause();a.currentTime=0}catch{}})}}
function useBandState(){const[state,setState]=useState(()=>{try{return migrate(JSON.parse(localStorage.getItem("bandcenter_v2")||"null"))}catch{return emptyState()}});const[cloud,setCloud]=useState("local");const loaded=useRef(false),client=useRef(null),timer=useRef(null);
 useEffect(()=>{async function init(){let current=state;try{const r=await fetch("data.json",{cache:"no-store"});if(r.ok){const legacy=migrate(await r.json());if(!current.students.length)current=legacy;}}catch{}const url=CFG.supabaseUrl,key=CFG.supabaseAnonKey;if(url&&key&&window.supabase){try{client.current=window.supabase.createClient(url,key);const{data,error}=await client.current.from("bandcenter_state").select("payload,updated_at").eq("id","okeene-bandcenter").maybeSingle();if(!error&&data?.payload&&Object.keys(data.payload).length){const remote=migrate(data.payload);if((remote.meta?.updatedAt||0)>(current.meta?.updatedAt||0))current=remote;}setCloud(error?"error":"online");}catch{setCloud("error")}}setState(current);loaded.current=true;}init();},[]);
 useEffect(()=>{if(!loaded.current)return;const next={...state,meta:{...state.meta,updatedAt:Date.now()}};localStorage.setItem("bandcenter_v2",JSON.stringify(next));if(client.current){clearTimeout(timer.current);timer.current=setTimeout(async()=>{setCloud("syncing");const{error}=await client.current.from("bandcenter_state").upsert({id:"okeene-bandcenter",payload:next,updated_at:new Date().toISOString()});setCloud(error?"error":"online")},700)}},[state]);return[state,setState,cloud];}
const Button=({children,className="",...p})=><button className={`btn ${className}`} {...p}>{children}</button>;
const Input=p=><input className="input" {...p}/>;const Select=p=><select className="select" {...p}/>;
function Logo(){return <div className="bc-mark">BC</div>}
function Header({state,cloud,isAdmin,setAdmin}){const ensemble=state.settings.activeEnsemble;const board=allStats(state,ensemble);const champ=[...(state.tournaments||[])].filter(t=>!t.archived&&t.ensemble===ensemble&&t.championId).sort((a,b)=>(b.date||"").localeCompare(a.date||""))[0];const champS=champ&&state.students.find(s=>s.id===champ.championId);return <><div className="topbar"><div className="brand-row"><Logo/><div><div className="brand-title">BandCenter</div><div className="brand-sub">{state.meta.schoolName}</div></div><div className="spacer"/><div className="cloud-box"><span className={`cloud-dot ${cloud==="online"?"online":""}`}/><span className="brand-sub">{cloud==="online"?"Cloud synced":cloud==="syncing"?"Syncing":"Local mode"}</span></div><Button className="sm" onClick={()=>{if(isAdmin)setAdmin(false);else if(prompt("Commissioner passcode")===String(CFG.adminPasscode||"admin"))setAdmin(true)}}>{isAdmin?"Exit Commissioner":"Commissioner"}</Button></div></div><div className="ticker"><div className="ticker-label">BANDCENTER</div><div className="ticker-track"><span>{state.meta.seasonName}</span><span>{board[0]?`POWER RANKING #1: ${board[0].student.moniker} • ${board[0].points} PTS`:"THE SEASON STARTS HERE"}</span><span>{champS?`DEFENDING CHAMPION: ${champS.moniker}`:"CHAMPIONSHIP BELT VACANT"}</span><span>PLAYING CHALLENGES ARE OPTIONAL • EVERY ATTEMPT CAN BUILD YOUR SEASON</span></div></div></>}
function Broadcast({state,audio}){const ensemble=state.settings.activeEnsemble,b=allStats(state,ensemble),leader=b[0],most=b.slice().sort((a,c)=>(c.best-c.history[0]?.score||0)-(a.best-a.history[0]?.score||0))[0];const champs=(state.tournaments||[]).filter(t=>!t.archived&&t.ensemble===ensemble&&t.championId);const champ=champs.sort((a,b)=>(b.date||"").localeCompare(a.date||""))[0];const cs=champ&&state.students.find(s=>s.id===champ.championId);return <div className="grid grid-2"><div className="hero"><div className="hero-inner"><div className="hero-kicker">Live from the Barking Battalion</div><h1>{state.meta.seasonName}</h1><p>Playing tests are now voluntary scoring opportunities. Build a résumé, climb the rankings, earn a seed, and make a run when Band Madness arrives.</p><div style={{display:"flex",gap:8,flexWrap:"wrap"}} className="no-print"><Button className="gold" onClick={()=>audio.play("espn")}>▶ Broadcast Theme</Button><Button onClick={()=>audio.play("applause")}>👏 Crowd</Button></div></div></div><div className="scoreboard"><div className="score-tile"><div className="score-num">{b.length}</div><div className="score-label">Active Players</div></div><div className="score-tile"><div className="score-num">{state.challenges.filter(c=>!c.archived&&c.ensemble===ensemble).length}</div><div className="score-label">Challenges</div></div><div className="score-tile"><div className="score-num">{state.tournaments.filter(t=>!t.archived&&t.ensemble===ensemble).length}</div><div className="score-label">Tournaments</div></div><div className="score-tile"><div className="score-num">{b.reduce((n,x)=>n+x.attempts,0)}</div><div className="score-label">Attempts</div></div></div><div className="broadcast-card"><div className="broadcast-label">Top Story</div><div className="headline">{leader?`${leader.student.moniker} owns the top spot`:"The standings are wide open"}</div><p>{leader?`${leader.points} championship points • career high ${leader.best.toFixed(1)} • ${leader.wins} tournament wins`:"Enter the first Playing Challenge score to start the season."}</p></div><div className="broadcast-card"><div className="broadcast-label">Championship Desk</div><div className="headline">{cs?`${cs.moniker} is the one to beat`:"The crown is waiting"}</div><p>{cs?`${champ.name} champion • ${champ.date}`:"Create a tournament in Commissioner Mode and let the bracket tell the story."}</p></div></div>}
function Standings({state}){const b=allStats(state,state.settings.activeEnsemble);return <div className="card"><div className="card-head"><div className="card-title">BandCenter Power Rankings</div></div><div>{b.map((x,i)=><div className={`leader-row ${i<3?"top3":""} rise`} key={x.student.id}><div className="rank">#{i+1}</div><div><div className="moniker">{x.student.moniker}</div><div className="realname">{x.student.instrument} • {x.attempts} challenge attempts • high {x.best?x.best.toFixed(1):"—"}</div></div><div className="pts">{x.points} <small>PTS</small></div></div>)}{!b.length&&<div className="empty-state">No active players in this ensemble yet.</div>}</div></div>}
function badges(x){const out=[];if(x.titles)out.push("🏆 Champion");if(x.best>=10)out.push("💯 Perfect 10");if(x.maxStreak>=3)out.push("🔥 Hot Streak");if(x.runnerUps)out.push("🥈 Finalist");if(x.wins>=3)out.push("⚡ Bracket Buster");if(x.pb>=3)out.push("📈 Riser");if(x.points>=500)out.push("⭐ 500 Club");return out;}
function PlayerCards({state}){const b=allStats(state,state.settings.activeEnsemble);return <div className="grid grid-3">{b.map(x=><div className="student-card" key={x.student.id}><div className="student-card-top"><div className="instrument-tag">{x.student.instrument}</div><div className="moniker" style={{fontSize:25,marginTop:7}}>{x.student.moniker}</div></div><div className="student-card-body"><div className="stat-row"><div className="mini-stat"><b>{x.points}</b><span>Points</span></div><div className="mini-stat"><b>{x.best?x.best.toFixed(1):"—"}</b><span>Career High</span></div><div className="mini-stat"><b>{x.wins}</b><span>Bracket Wins</span></div></div><div className="achievement-grid" style={{marginTop:12}}>{badges(x).map(b=><span className="badge gold" key={b}>{b}</span>)}{!badges(x).length&&<span className="badge">Season in progress</span>}</div></div></div>)}</div>}
function Roster({state,setState}){const ensemble=state.settings.activeEnsemble;const[form,setForm]=useState({name:"",instrument:"Flute"});const list=state.students.filter(s=>s.ensemble===ensemble);const patch=(id,p)=>setState(q=>({...q,students:q.students.map(s=>s.id===id?{...s,...p}:s)}));return <><div className="card"><div className="card-head"><div className="card-title">Add Player</div></div><div className="card-body field-row"><div><label>Real name</label><Input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div><label>Instrument</label><Select value={form.instrument} onChange={e=>setForm({...form,instrument:e.target.value})}>{INSTRUMENTS.map(i=><option key={i}>{i}</option>)}</Select></div><div style={{alignSelf:"end"}}><Button className="primary" onClick={()=>{if(!form.name.trim())return;const s={id:uid(),name:form.name.trim(),instrument:form.instrument,ensemble,active:true,bandBucks:0};s.moniker=generatedMoniker(s);setState(q=>({...q,students:[...q.students,s]}));setForm({name:"",instrument:"Flute"})}}>Add to League</Button></div></div></div><div className="card"><div className="card-head"><div className="card-title">Player Registry • {ensemble}</div></div><div className="table-wrap"><table className="table"><thead><tr><th>Player</th><th>Moniker</th><th>Instrument</th><th>Currency</th><th>Status</th><th>Actions</th></tr></thead><tbody>{list.map(s=><tr key={s.id}><td><Input value={s.name} onChange={e=>patch(s.id,{name:e.target.value})}/></td><td><Input value={s.moniker} onChange={e=>patch(s.id,{moniker:e.target.value})}/></td><td><Select value={s.instrument} onChange={e=>patch(s.id,{instrument:e.target.value})}>{INSTRUMENTS.map(i=><option key={i}>{i}</option>)}</Select></td><td><Input type="number" value={s.bandBucks||0} onChange={e=>patch(s.id,{bandBucks:Number(e.target.value)||0})}/></td><td><span className="chip">{s.active!==false?"Active":"Inactive"}</span></td><td style={{whiteSpace:"nowrap"}}><Button className="sm" onClick={()=>patch(s.id,{moniker:generatedMoniker(s,Math.random())})}>🎲 Regenerate</Button> <Button className="sm" onClick={()=>patch(s.id,{active:s.active===false})}>{s.active===false?"Activate":"Bench"}</Button> <Button className="sm red" onClick={()=>confirm(`Delete ${s.name}?`)&&setState(q=>({...q,students:q.students.filter(x=>x.id!==s.id)}))}>Delete</Button></td></tr>)}</tbody></table></div></div></>}
function scoreAwardPreview(state,challenge,studentId,value){const old=challenge.scores?.[studentId];const temp={...state,challenges:state.challenges.map(c=>c.id===challenge.id?{...c,scores:{...c.scores,[studentId]:value}}:c)};return challengePoints(temp,studentId).total-challengePoints({...temp,challenges:temp.challenges.map(c=>c.id===challenge.id?{...c,scores:{...c.scores,[studentId]:old}}:c)},studentId).total;}
function Challenges({state,setState,audio,toast}){const ensemble=state.settings.activeEnsemble;const[name,setName]=useState("");const list=state.challenges.filter(c=>!c.archived&&c.ensemble===ensemble);const students=state.students.filter(s=>s.active!==false&&s.ensemble===ensemble).sort((a,b)=>a.instrument.localeCompare(b.instrument)||a.name.localeCompare(b.name));const setScore=(c,s,val)=>{const n=val===""?"":clamp(val,0,10);const delta=scoreAwardPreview(state,c,s.id,n);setState(q=>({...q,challenges:q.challenges.map(x=>x.id===c.id?{...x,scores:{...(x.scores||{}),[s.id]:n}}:x)}));if(n!==""){audio.play(n>=9.5?"victory":"deep");toast(`${s.moniker}: ${Number(n).toFixed(1)} • ${delta>=0?"+":""}${delta} season points`)}};return <><div className="card"><div className="card-head"><div className="card-title">Create Optional Playing Challenge</div></div><div className="card-body field-row"><div><label>Challenge name</label><Input value={name} onChange={e=>setName(e.target.value)} placeholder="Concert F Challenge"/></div><div><label>Date</label><Input id="new-date" type="date" defaultValue={today()}/></div><div style={{alignSelf:"end"}}><Button className="primary" onClick={()=>{if(!name.trim())return;const d=document.getElementById("new-date").value||today();setState(q=>({...q,challenges:[...q.challenges,{id:uid(),name:name.trim(),date:d,ensemble,scores:{},archived:false}]}));setName("")}}>Open Challenge</Button></div></div></div>{list.map(c=><div className="card" key={c.id}><div className="card-head"><div><div className="card-title">{c.name}</div><div className="realname">{c.date} • OPTIONAL SCORING OPPORTUNITY</div></div><div className="spacer"/><Button className="sm" onClick={()=>setState(q=>({...q,challenges:q.challenges.map(x=>x.id===c.id?{...x,archived:true}:x)}))}>Archive</Button></div><div className="table-wrap"><table className="table"><thead><tr><th>Player</th><th>Instrument</th><th>Score / 10</th><th>Season Points</th><th>Career High</th></tr></thead><tbody>{students.map(s=>{const st=statsFor(state,s);return <tr key={s.id}><td><b>{s.moniker}</b><div className="realname">{s.name}</div></td><td>{s.instrument}</td><td><Input className="challenge-score" type="number" min="0" max="10" step=".1" value={c.scores?.[s.id]??""} onChange={e=>setScore(c,s,e.target.value)}/></td><td><b>{st.points}</b></td><td>{st.best?st.best.toFixed(1):"—"}</td></tr>})}</tbody></table></div></div>)}</>}
function seedOrder(state,ensemble,size){const ranked=allStats(state,ensemble).map(x=>x.student.id);const active=state.students.filter(s=>s.active!==false&&s.ensemble===ensemble).map(s=>s.id);const uniq=[...ranked,...active.filter(x=>!ranked.includes(x))].slice(0,size);return uniq;}
function makeRounds(ids,size){const arr=[...ids];while(arr.length<size)arr.push(null);const rounds=[];let count=size/2;for(let r=0;count>=1;r++){const matches=Array.from({length:count},(_,i)=>({id:uid(),p1:r===0?arr[i*2]:null,p2:r===0?arr[i*2+1]:null,winner:null}));rounds.push({name:r===Math.log2(size)-1?"Championship":r===Math.log2(size)-2?"Final Four":r===Math.log2(size)-3?"Elite Eight":`Round ${r+1}`,matches});count/=2;}return rounds;}
function propagate(rounds){const out=rounds.map(r=>({...r,matches:r.matches.map(m=>({...m}))}));for(let r=0;r<out.length-1;r++){out[r].matches.forEach((m,i)=>{const next=out[r+1].matches[Math.floor(i/2)];const slot=i%2===0?"p1":"p2";next[slot]=m.winner||null;if(!m.winner&&next.winner===next[slot])next.winner=null;});}return out;}
function TournamentStudio({state,setState,audio,toast}){const ensemble=state.settings.activeEnsemble;const[name,setName]=useState("Band Madness");const[size,setSize]=useState(32);const list=state.tournaments.filter(t=>!t.archived&&t.ensemble===ensemble);function create(){const ids=seedOrder(state,ensemble,size);const t={id:uid(),name,date:today(),ensemble,size,rounds:makeRounds(ids,size),championId:null,archived:false};setState(q=>({...q,tournaments:[...q.tournaments,t]}));audio.play("start")}
 function win(tid,ri,mi,w){setState(q=>({...q,tournaments:q.tournaments.map(t=>{if(t.id!==tid)return t;let rounds=t.rounds.map(r=>({...r,matches:r.matches.map(m=>({...m}))}));rounds[ri].matches[mi].winner=w;rounds=propagate(rounds);const last=rounds.at(-1).matches[0];const champion=last?.winner||null;if(champion&&champion!==t.championId){const s=q.students.find(x=>x.id===champion);setTimeout(()=>{audio.play("victory");toast(`${s?.moniker||"Champion"} WINS ${t.name.toUpperCase()}!`)},50)}return{...t,rounds,championId:champion}})}))}
 return <><div className="card"><div className="card-head"><div className="card-title">Tournament Selection Studio</div></div><div className="card-body field-row"><div><label>Tournament name</label><Input value={name} onChange={e=>setName(e.target.value)}/></div><div><label>Bracket size</label><Select value={size} onChange={e=>setSize(Number(e.target.value))}>{SIZES.map(n=><option key={n} value={n}>{n}-Seed</option>)}</Select></div><div style={{alignSelf:"end"}}><Button className="red" onClick={create}>📺 Reveal Bracket</Button></div><div style={{alignSelf:"end"}}><Button onClick={()=>audio.play("espn")}>Theme</Button></div></div><div className="card-body commissioner-note">Every active student may enter. Power Rankings determine the seed order. Students with no challenge scores simply enter lower in the field rather than being excluded.</div></div>{list.map(t=><Tournament key={t.id} state={state} t={t} editable onWin={win}/>)}</>}
function Tournament({state,t,editable,onWin}){const s=id=>state.students.find(x=>x.id===id);return <div className="card"><div className="card-head"><div><div className="card-title">{t.name}</div><div className="realname">{t.date} • {t.size}-Seed Championship</div></div></div>{t.championId&&<div className="card-body"><div className="champion-banner"><div className="broadcast-label">Reigning Champion</div><h2>{s(t.championId)?.moniker||"Champion"}</h2><b>{s(t.championId)?.instrument}</b></div></div>}<div className="bracket">{(t.rounds||[]).map((r,ri)=><div className="round" key={ri}><div className="round-title">{r.name}</div>{r.matches.map((m,mi)=><div className="match" key={m.id}>{[m.p1,m.p2].map((id,slot)=><div key={slot} className={`competitor ${m.winner===id&&id?"winner":""} ${!id?"empty":""}`} onClick={()=>editable&&id&&onWin(t.id,ri,mi,id)}><span className="seed">{id?allStats(state,t.ensemble).findIndex(x=>x.student.id===id)+1:""}</span><span style={{flex:1,fontWeight:850}}>{id?s(id)?.moniker||"Unknown":"TBD"}</span>{m.winner===id&&id?<b>✓</b>:null}</div>)}</div>)}</div>)}</div></div>}
function Brackets({state}){const list=state.tournaments.filter(t=>!t.archived&&t.ensemble===state.settings.activeEnsemble);return <>{list.map(t=><Tournament key={t.id} state={state} t={t}/>)}{!list.length&&<div className="empty-state">No live tournament yet.</div>}</>}
function Settings({state,setState}){const r=state.rules;const fields=[['attempt','Any attempt'],['s7','7.0–7.9'],['s8','8.0–8.9'],['s9','9.0–9.49'],['s95','9.5–9.99'],['s10','Perfect 10'],['personalBest','New personal best'],['improveOne','Improve by 1+'],['tournamentEntry','Tournament entry'],['roundWin','Each round win'],['eliteEight','Elite Eight'],['finalFour','Final Four'],['runnerUp','Runner-up'],['champion','Champion']];return <div className="grid grid-2"><div className="card"><div className="card-head"><div className="card-title">Season Identity</div></div><div className="card-body grid"><div><label>Season</label><Input value={state.meta.seasonName} onChange={e=>setState(q=>({...q,meta:{...q.meta,seasonName:e.target.value}}))}/></div><div><label>School / Program</label><Input value={state.meta.schoolName} onChange={e=>setState(q=>({...q,meta:{...q.meta,schoolName:e.target.value}}))}/></div><div><label>Sound volume</label><Input type="range" min="0" max="1" step=".05" value={state.settings.volume} onChange={e=>setState(q=>({...q,settings:{...q.settings,volume:Number(e.target.value)}}))}/></div></div></div><div className="card"><div className="card-head"><div className="card-title">Championship Point Rules</div></div><div className="card-body grid grid-2">{fields.map(([k,label])=><div key={k}><label>{label}</label><Input type="number" value={r[k]} onChange={e=>setState(q=>({...q,rules:{...q.rules,[k]:Number(e.target.value)||0}}))}/></div>)}</div></div><div className="card"><div className="card-head"><div className="card-title">Cloud Setup</div></div><div className="card-body"><p>BandCenter runs locally now. For multi-device syncing, create a Supabase project, run <b>supabase-schema.sql</b>, then place the Project URL and anon key in <b>bandcenter-config.js</b>.</p><p className="commissioner-note">The classroom broadcast defaults to monikers. Commissioner Mode contains real student names and should not be projected publicly.</p></div></div><div className="card"><div className="card-head"><div className="card-title">Data Safety</div></div><div className="card-body"><Button onClick={()=>{const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='bandcenter-backup.json';a.click()}}>Export Backup</Button> <Button className="red" onClick={()=>confirm('Clear local BandCenter 2 data?')&&(localStorage.removeItem('bandcenter_v2'),location.reload())}>Reset Local Cache</Button></div></div></div>}
function App(){const[state,setState,cloud]=useBandState();const[admin,setAdmin]=useState(false);const[tab,setTab]=useState("Broadcast");const[toastMsg,setToast]=useState("");const audio=useAudio(state);const toast=msg=>{setToast(msg);setTimeout(()=>setToast(""),3200)};const publicTabs=["Broadcast","Standings","Player Cards","Brackets"],adminTabs=["Roster","Challenges","Tournament Studio","Settings"],tabs=admin?adminTabs:publicTabs;useEffect(()=>{if(!tabs.includes(tab))setTab(admin?"Roster":"Broadcast")},[admin]);const ensemble=state.settings.activeEnsemble;return <div className="app-shell"><Header state={state} cloud={cloud} isAdmin={admin} setAdmin={setAdmin}/><main className="container"><div style={{display:"flex",gap:10,alignItems:"center",flexWrap:"wrap"}} className="no-print"><div style={{minWidth:230}}><Select value={ensemble} onChange={e=>setState(q=>({...q,settings:{...q.settings,activeEnsemble:e.target.value}}))}>{ENSEMBLES.map(e=><option key={e}>{e}</option>)}</Select></div><div className="nav-tabs">{tabs.map(t=><button key={t} className={`nav-btn ${tab===t?"active":""}`} onClick={()=>setTab(t)}>{t}</button>)}</div></div>{tab==="Broadcast"&&<Broadcast state={state} audio={audio}/>} {tab==="Standings"&&<Standings state={state}/>} {tab==="Player Cards"&&<PlayerCards state={state}/>} {tab==="Brackets"&&<Brackets state={state}/>} {tab==="Roster"&&<Roster state={state} setState={setState}/>} {tab==="Challenges"&&<Challenges state={state} setState={setState} audio={audio} toast={toast}/>} {tab==="Tournament Studio"&&<TournamentStudio state={state} setState={setState} audio={audio} toast={toast}/>} {tab==="Settings"&&<Settings state={state} setState={setState}/>}</main>{toastMsg&&<div className="toast">{toastMsg}</div>}</div>}
ReactDOM.createRoot(document.getElementById('root')).render(<App/>);